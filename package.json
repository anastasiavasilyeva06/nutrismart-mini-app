{
  "name": "nutrismart-mini-app",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "vercel-build": "prisma generate && prisma db push && npm run -w frontend build",
    "build": "npm run -w frontend build",
    "dev": "npm run -w frontend dev",
    "lint": "npm run -w frontend lint"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.0",
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}

